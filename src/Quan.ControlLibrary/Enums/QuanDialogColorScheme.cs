namespace Quan.ControlLibrary.Enums;

/// <summary>
/// An enum representing the different choices for a color scheme in a Metro Dialog.
/// </summary>
public enum QuanDialogColorScheme
{
    Theme = 0,
    Accented = 1,
    Inverted = 2
}